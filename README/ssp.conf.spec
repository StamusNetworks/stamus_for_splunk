[config]
api_key = <value>
* The user token generated on Stamus Security Platform.

base_url = <value>
* The base URL of Stamus Security Platform.

check_tls = <value>
* A boolean (yes|no) to indicate if TLS certificates should be checked when connecting to Stamus Security Platform.
