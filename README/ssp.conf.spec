[config]
api_key = <value>
* The user token generated on Scirius Security Platform.

base_url = <value>
* The base URL of Scirius Security Platform.

check_tls = <value>
* A boolean (yes|no) to indicate if TLS certificates should be checked when connecting to Scirius Security Platform.
